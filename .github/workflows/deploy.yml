name: Deploy Rata Audaz Final

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      # ESTO ES LO IMPORTANTE: Creamos el archivo DIRECTAMENTE con los secretos
      - name: Crear config.js real
        run: |
          echo "const CONFIG = { 
            WEATHER_API_KEY: '${{ secrets.WEATHER_API_KEY }}', 
            CITY: '${{ secrets.CITY }}', 
            UNITS: 'metric', 
            LANG: 'es' 
          };" > config.js

      - name: Deploy a GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: .        # Sube todo el contenido de la raíz
          branch: gh-pages # A la rama de la web
          clean: true
